---
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">
        <span class="brand-name">SAVE INN</span> - Ihre Werkstatt in Hamburg
      </h1>
      <p class="hero-subtitle">
        Professionelle Autoreparatur & Service seit vielen Jahren
      </p>
      <p class="hero-features">
        <span class="feature">✓ Top-Service</span>
        <span class="feature">✓ Inspektion</span>
        <span class="feature">✓ Ölwechsel</span>
        <span class="feature">✓ Reparaturen</span>
        <span class="feature">✓ Reifenwechsel</span>
      </p>
      <div class="hero-cta">
        <a href="tel:040773192" class="btn btn-primary btn-large">
          📞 Jetzt anrufen
        </a>
        <a href="#leistungen" class="btn btn-secondary btn-large">
          Leistungen ansehen
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(135deg, var(--color-gray-lightest) 0%, var(--color-white) 100%);
    padding: var(--spacing-xxl) 0;
    position: relative;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%230D47A1' fill-opacity='0.03'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    opacity: 0.5;
    pointer-events: none;
  }

  .hero-content {
    max-width: 800px;
    text-align: center;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-size: 3rem;
    margin-bottom: var(--spacing-md);
    color: var(--color-gray-dark);
  }

  .brand-name {
    color: var(--color-red-primary);
    font-weight: 800;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-gray-medium);
    margin-bottom: var(--spacing-lg);
  }

  .hero-features {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
    font-size: 1rem;
    color: var(--color-gray-dark);
  }

  .feature {
    font-weight: 600;
  }

  .hero-cta {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero {
      padding: var(--spacing-xl) 0;
    }

    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
    }

    .hero-features {
      flex-direction: column;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .hero-cta {
      flex-direction: column;
    }

    .btn-large {
      width: 100%;
      max-width: 300px;
    }
  }
</style>
